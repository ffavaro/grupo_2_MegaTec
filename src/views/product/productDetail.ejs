<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head')%>
    <link href="/css/styles-productDetail.css" rel="stylesheet">
</head>
    <body>
        <%- include('../partials/header')%>
    <main>
        <section>
            <article>
                <div class="product">
                    <h3 class="descripciones"> <%= product.name %> </h3>
                    <img src="/images/product/<%= product.image %> " alt="<%= product.name %> "> </img>
                </div>
            </article>
        </section>
        <section>
            <article>
                <div>
                    <h2>Especificaciones</h2>
                    <h3 class="descripciones"> <%= product.description%></h3>
                    <br>
                    <h2>Precio</h2>
                    <p class="precios">$<%= product.price%></p>
                    <p class="descuentos"> Descuento <%= product.discount%> %</p>
                </div>
                <div class="buttons">
                    <button class="button-save">
                        Comprar
                    </button>
                    <form  method="post" action="/product/delete/<%= product.id %>?_method=DELETE">
                        <button type="submit" class="button-delete">ELIMINAR</button>
                    </form>
                    <form  method="get" action="/product/edit/<%= product.id %>">
                        <button type="submit" class="button-delete">Editar</button>
                    </form>
                </div>
            </article>
            <article>
                <h2>Productos Nuevo</h2>
                <div class="other-produc">
                    <% for( productNew of listProduct) { %>   
                            <img src="/images/product/<%=productNew.image%>" alt="<%= productNew.name %>"></img>           
                    <% } %>
                </div>
            </article>
        </section>
    </main>
    <%- include('../partials/footer')%>
</body>

</html>